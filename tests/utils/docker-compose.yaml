version: '3'

services:
    first_service:
        container_name: service1
        ports:
            - 80:80
            - 443:443
        volumes:
            # this is a comment
            - ./bind_mount1:/container1/path
            - named_volume1:/another/container1/path
    
    second_service:
        image: source/image
        volumes:
            - named_volume2:/container2/path # with a comment
            - ./bind_mount2:/another/container2/path

networks:
    - a_random_network